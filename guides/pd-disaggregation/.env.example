# Copy to .env in this directory (optional if you export vars in the shell).
# See README.md (AKS + just workflow).

NAMESPACE=your-user-dev
HF_TOKEN=hf_...

# Optional: tensor parallelism and replicas for ms-pd on AKS (helmfile --set via env).
# Defaults match ms-pd/values_aks.yaml if unset.
# PREFILL_TP=8
# DECODE_TP=8
# PREFILL_REPLICAS=1
# DECODE_REPLICAS=1
